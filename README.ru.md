# 🌍 Localeasy

> Простой и мощный CLI инструмент для управления файлами локализации

[English](README.en.md) | Русский

[![Node.js](https://img.shields.io/badge/Node.js-18+-green.svg)](https://nodejs.org/)
[![TypeScript](https://img.shields.io/badge/TypeScript-5.9-blue.svg)](https://www.typescriptlang.org/)
[![License](https://img.shields.io/badge/license-MIT-blue.svg)](LICENSE)

Localeasy - это удобный инструмент командной строки для управления файлами локализации в формате JSON. Он позволяет быстро инициализировать проект локализации, добавлять, удалять и сортировать ключи перевода.

## ✨ Особенности

- 🚀 **Быстрая инициализация** - создание структуры локализации одной командой
- ➕ **Простое добавление ключей** - добавление переводов в один или несколько файлов
- 🗑️ **Безопасное удаление** - удаление ключей с подтверждением
- 📊 **Автоматическая сортировка** - поддержание порядка ключей в файлах
- ✅ **Валидация** - проверка формата ключей и языковых кодов
- 🔄 **Пакетная обработка** - работа с несколькими файлами одновременно
- 🎯 **TypeScript** - полная поддержка типов

## 📦 Установка

### Глобальная установка

```bash
npm install -g @localeasy/cli
```

### Локальная установка

```bash
npm install --save-dev @localeasy/cli
```

### Использование через npx

```bash
npx @localeasy/cli init
```

## 🚀 Быстрый старт

### 1. Инициализация проекта

```bash
localeasy init
```

Это создаст директорию `./locales` с файлами `en.json` и `ru.json`:

```json
// locales/en.json
{
  "welcome": "Welcome to en",
  "hello": "Hello in en"
}
```

### 2. Добавление переводов

```bash
# Добавить ключ в один файл
localeasy add -f ./locales/en.json -k greeting -v "Hello, World!"

# Добавить ключ во все файлы в директории
localeasy add -d ./locales --all -k greeting -v "Hello, World!" --force
```

### 3. Удаление ключей

```bash
localeasy delete -f ./locales/en.json -k greeting
```

### 4. Сортировка ключей

```bash
# Сортировать один файл
localeasy sort -f ./locales/en.json

# Сортировать все файлы в директории
localeasy sort -d ./locales

# Предварительный просмотр (dry-run)
localeasy sort -d ./locales --dry-run
```

## 📚 Команды

### `init` - Инициализация проекта

Создает структуру локализации с файлами для указанных языков.

```bash
localeasy init [options]
```

**Опции:**
- `-d, --directory <path>` - Директория для инициализации (по умолчанию: `./locales`)
- `-l, --languages <languages>` - Список языков через запятую (по умолчанию: `en,ru`)

**Примеры:**
```bash
# Базовый вариант
localeasy init

# Кастомная директория и языки
localeasy init -d ./src/locales -l en,ru,fr,de
```

### `add` - Добавление ключа перевода

Добавляет новый ключ перевода в один или несколько файлов.

```bash
localeasy add [options]
```

**Опции:**
- `-f, --file <path>` - Путь к файлу локализации (по умолчанию: `./locales/en.json`)
- `-d, --directory <path>` - Директория с файлами локализации
- `-k, --key <key>` - Ключ перевода (обязательно)
- `-v, --value <value>` - Значение перевода (обязательно)
- `--all` - Добавить ключ во все файлы в директории
- `--force` - Принудительно перезаписать существующий ключ

**Примеры:**
```bash
# Добавить в один файл
localeasy add -f ./locales/en.json -k welcome -v "Welcome"

# Добавить во все файлы в директории
localeasy add -d ./locales --all -k welcome -v "Welcome"

# Перезаписать существующий ключ
localeasy add -f ./locales/en.json -k welcome -v "New Welcome" --force
```

**Формат ключей:**
- Разрешены только буквы, цифры, точки, подчеркивания и дефисы
- Примеры валидных ключей: `welcome`, `user.name`, `button.submit`, `page-title`

### `delete` - Удаление ключа

Удаляет ключ перевода из файла с подтверждением.

```bash
localeasy delete [options]
```

**Опции:**
- `-f, --file <path>` - Путь к файлу локализации (по умолчанию: `./locales/en.json`)
- `-k, --key <key>` - Ключ для удаления (обязательно)
- `--force` - Удалить без подтверждения

**Примеры:**
```bash
# Удалить с подтверждением
localeasy delete -f ./locales/en.json -k welcome

# Удалить без подтверждения
localeasy delete -f ./locales/en.json -k welcome --force
```

### `sort` - Сортировка ключей

Сортирует ключи перевода в файлах в алфавитном порядке.

```bash
localeasy sort [options]
```

**Опции:**
- `-f, --file <path>` - Путь к файлу для сортировки
- `-d, --directory <path>` - Директория с файлами для сортировки
- `--dry-run` - Показать что будет отсортировано без изменений

**Примеры:**
```bash
# Сортировать один файл
localeasy sort -f ./locales/en.json

# Сортировать все файлы в директории
localeasy sort -d ./locales

# Предварительный просмотр
localeasy sort -d ./locales --dry-run
```

## 🏗️ Структура проекта

Localeasy состоит из двух основных пакетов:

```
localeasy/
├── packages/
│   ├── core/          # Основная логика работы с локализацией
│   │   ├── src/
│   │   │   ├── utils/     # Утилиты для работы с файлами и валидации
│   │   │   └── types/     # TypeScript типы
│   │   └── dist/          # Скомпилированный код
│   │
│   └── cli/            # CLI интерфейс
│       ├── src/
│       │   └── commands/  # Команды CLI
│       └── dist/          # Скомпилированный код
│
├── package.json
└── README.md
```

### Пакеты

#### `@localeasy/core`

Основной пакет с логикой работы с локализацией.

#### `@localeasy/cli`

CLI инструмент для управления локализацией из командной строки.

## 🎯 Лучшие практики

1. **Используйте структурированные ключи**: `page.home.title`, `button.submit` вместо `homeTitle`, `submitButton`
2. **Сортируйте файлы регулярно**: это помогает находить ключи и избегать дублирования
3. **Используйте `--dry-run`**: перед массовыми операциями проверяйте результат
4. **Валидация ключей**: используйте только допустимые символы (буквы, цифры, точки, подчеркивания, дефисы)
5. **Версионирование**: храните файлы локализации в системе контроля версий

## 🙏 Благодарности

Спасибо всем, кто использует и вносит вклад в развитие Localeasy!

---

**Сделано с ❤️ для разработчиков**